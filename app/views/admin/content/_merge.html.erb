<% if @merge %>
    <h4><%= _("Merge Articles") %> </h4>
    <%= form_tag("/admin/content",:action => "merge_articles", :id => @article.id ) do %>
      <p >Article ID <%= text_field_tag 'merge_id' %> </p>
      <%= submit_tag "Merge"%>
    <% end %>
<% end %>